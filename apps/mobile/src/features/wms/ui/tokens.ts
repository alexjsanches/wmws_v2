import { space } from '@wms/theme'

export const wmsUiTokens = {
  screenPadding: space.lg,
  screenBottomPadding: space.xl * 2,
  sectionGap: space.md,
  cardPadding: space.md,
  modalPadding: space.lg,
  modalMaxHeight: '78%' as const,
}
